services:
  auth-service:
    container_name: auth_service_dev
    build:
      context: .
      dockerfile: apps/auth-service/Dockerfile.dev
    env_file:
      - .env
    volumes:
      - ./apps/auth-service:/app/apps/auth-service
      - ./apps/core:/app/apps/core
      - /app/apps/auth-service/node_modules
      - /app/apps/core/node_modules
      - /app/apps/auth-service/dist
      - /app/apps/core/dist
    ports:
      - "3000:3000"
    networks:
      - transcendence
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
  user-service:
    container_name: user_service_dev
    build:
      context: .
      dockerfile: apps/user-service/Dockerfile.dev
    env_file:
      - .env
    volumes:
      - ./apps/user-service:/app/apps/user-service
      - ./apps/core:/app/apps/core
      - /app/apps/user-service/node_modules
      - /app/apps/core/node_modules
      - /app/apps/user-service/dist
      - /app/apps/core/dist
    ports:
      - "3001:3001"
    networks:
      - transcendence
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
  notification-service:
    container_name: notification_service_dev
    build:
      context: .
      dockerfile: apps/notification-service/Dockerfile.dev
    env_file:
      - .env
    volumes:
      - ./apps/notification-service:/app/apps/notification-service
      - ./apps/core:/app/apps/core
      - /app/apps/notification-service/node_modules
      - /app/apps/core/node_modules
      - /app/apps/notification-service/dist
      - /app/apps/core/dist
    ports:
      - "3002:3002"
    networks:
      - transcendence
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true

networks:
  transcendence:
    driver: bridge
